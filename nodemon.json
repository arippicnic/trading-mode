{
  "verbose": true,
  "ignore": ["node_modules", ".next"],
  "watch": ["server/**/*"],
  "ext": "ts json",
  "exec": "ts-node --project tsconfig.server.json -r tsconfig-paths/register -r dotenv/config --files server/index.ts"
}
